services:
  bandersnatch-db:
    image: postgres:16
    container_name: bandersnatch-db
    environment:
      POSTGRES_DB: bandersnatch
      POSTGRES_USER: tuckersoft
      POSTGRES_PASSWORD: colin1984
    ports:
      - "5432:5432"
    volumes:
      - bandersnatch-db-data:/var/lib/postgresql/data

volumes:
  bandersnatch-db-data:
